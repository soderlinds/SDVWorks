{
  "name": "sdv",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ sdv",
    "create-local": "graph create --node http://localhost:8020/ sdv",
    "remove-local": "graph remove --node http://localhost:8020/ sdv",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 sdv",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.69.2",
    "@graphprotocol/graph-ts": "0.32.0",
    "graphql-request": "^6.1.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
